<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>

  <Target Name="ModelGen" AfterTargets="BeforeCompile">
    <Exec Command="powershell -file &quot;$(SolutionDir)Go\build-all.ps1&quot;" />
  </Target>

</Project>
