apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    k8s-app: calico-node
  name: calico-node
  namespace: kube-system
spec:
  endpoints:
    - interval: 5m
      path: /metrics
      scrapeTimeout: 30s
      targetPort: 9091
  jobLabel: calico-node
  namespaceSelector:
    matchNames:
      - kube-system
  selector:
    matchLabels:
      k8s-app: calico-node
