apiVersion: v1
kind: NeonClusterOperator
metadata:
  labels:
    {{- include "neon-cluster-operator.labels" . | nindent 4 }}
  name: neon-cluster-operator
dspec:
  certificateUpdateSchedule:
    controlPlane: '0 0 * * *'
    nodeCa: '0 0 * * *'

