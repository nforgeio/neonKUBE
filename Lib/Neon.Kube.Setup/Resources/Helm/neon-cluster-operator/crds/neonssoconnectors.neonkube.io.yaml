apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: neonssoconnectors.neonkube.io
spec:
  group: neonkube.io
  names:
    kind: NeonSsoConnector
    plural: neonssoconnectors
    shortNames: []
    singular: neonssoconnector
  scope: Cluster
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Specifies Neon SSO client settings.
        properties:
          spec:
            description: Configuration for backend connectors.
            properties:
              id:
                description: Connector ID
                type: string
              name:
                description: |-
                  Connector name.
                      information.
                type: string
              type:
                description: |-
                  Connector Type.
                      information.
                type: string
              config:
                description: Supports all classes in the .NET Framework class hierarchy and provides low-level services to derived classes. This is the ultimate base class of all classes in the .NET Framework; it is the root of the type hierarchy.
                properties: {}
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources: {}