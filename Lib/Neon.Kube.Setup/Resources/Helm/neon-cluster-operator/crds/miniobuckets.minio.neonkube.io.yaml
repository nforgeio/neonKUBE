apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: miniobuckets.minio.neonkube.io
spec:
  group: minio.neonkube.io
  names:
    kind: MinioBucket
    plural: miniobuckets
    shortNames: []
    singular: miniobucket
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Used for unit testing Kubernetes clients.
        properties:
          spec:
            description: The spec.
            properties:
              tenant:
                description: Represents text as a sequence of UTF-16 code units.
                type: string
              region:
                description: Represents text as a sequence of UTF-16 code units.
                type: string
              versioning:
                description: The bucket versioning mode.
                enum:
                - "off"
                - enabled
                - suspended
                type: string
              objectLocking:
                description: Represents a Boolean (true or false) value.
                type: boolean
              quota:
                description: Specifies bucket quota.
                properties:
                  limit:
                    description: Represents text as a sequence of UTF-16 code units.
                    type: string
                  hard:
                    description: Represents a Boolean (true or false) value.
                    type: boolean
                type: object
            type: object
          status:
            description: The status.
            properties:
              conditions:
                description: Represents a strongly typed list of objects that can be accessed by index. Provides methods to search, sort, and manipulate lists.
                items:
                  description: |-
                    Condition contains details for one aspect of the current state of this API
                     Resource.
                  properties:
                    lastTransitionTime:
                      description: Represents an instant in time, typically expressed as a date and time of day.
                      format: date-time
                      type: string
                    message:
                      description: Represents text as a sequence of UTF-16 code units.
                      type: string
                    observedGeneration:
                      description: Represents a value type that can be assigned null.
                      format: int64
                      type: integer
                    reason:
                      description: Represents text as a sequence of UTF-16 code units.
                      type: string
                    status:
                      description: Represents text as a sequence of UTF-16 code units.
                      type: string
                    type:
                      description: Represents text as a sequence of UTF-16 code units.
                      type: string
                  type: object
                type: array
              state:
                description: Represents text as a sequence of UTF-16 code units.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}