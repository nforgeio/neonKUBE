# Number of replicas to be deployed.
replicaCount: 1

# Container imagge details.

# Debugging image details (comment out for production).
image:
  organization: ghcr.io/neonkube-dev
  repository: neon-cluster-operator
  pullPolicy: Always
  tag: "neonkube-0.3.0-alpha"

