# Default values for neon-cluster-operator.

image:
  organization: neon-registry.node.local
  repository: neon-cluster-operator
  pullPolicy: IfNotPresent

logging:
  level: info

config:
  logPurgeInterval: 3600
  retentionDays: 3

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

nodeSelector:
  node-role.kubernetes.io/master: "true"
