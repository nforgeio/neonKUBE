apiVersion: v1
kind: Secret
metadata:
  name: grafana-env
  namespace: {{ .Release.Namespace }}
  annotations:
    reloader.stakater.com/match: "true"
stringData:
  GF_INSTALL_PLUGINS: {{ join "," .Values.plugins }}