# Default values for m3db-operator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

operator:
  name: m3db-operator
image:
  repository: quay.io/m3db/m3db-operator
  tag: v0.8.0
environment: production