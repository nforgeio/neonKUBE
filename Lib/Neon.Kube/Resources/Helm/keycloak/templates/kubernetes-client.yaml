apiVersion: keycloak.org/v1alpha1
kind: KeycloakClient
metadata:
  name: kubernetes
  labels:
    app: neon-sso
spec:
  realmSelector:
     matchLabels:
      app: neon-sso
  client:
    clientId: kubernetes