apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: minio-operator-dashboard-virtual-service
  namespace: neon-ingress
spec:
  gateways:
    - neoncluster-gateway
  hosts:
    - minio-operator.{{ .Values.clusterDomain }}
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: console.neon-monitor.svc.cluster.local
            port:
              number: 9090 
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: minio-console-dashboard-virtual-service
  namespace: neon-ingress
spec:
  gateways:
    - neoncluster-gateway
  hosts:
    - minio-console.{{ .Values.clusterDomain }}
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: minio-console.neon-system.svc.cluster.local
            port:
              number: 9090