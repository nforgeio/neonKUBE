apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: {{ .Values.clusterDomain }}
  namespace: neon-ingress
spec:
  commonName: {{ .Values.clusterDomain }}
  dnsNames:
    - {{ .Values.clusterDomain }}
    - "*.{{ .Values.clusterDomain }}"
  duration: 2160h0m0s
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: neon-acme
  keyAlgorithm: rsa
  keyEncoding: pkcs1
  keySize: 2048
  organization:
    - neoncluster.io
  privateKey: {}
  renewBefore: 360h0m0s
  secretName: {{ .Values.clusterDomain }}
  subject: {}
  usages:
    - server auth
    - client auth