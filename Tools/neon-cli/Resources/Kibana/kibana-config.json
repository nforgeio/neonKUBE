{
  "type": "config",
  "updated_at": "${TIMESTAMP}",
  "config": {
    "buildNum": "${BUILDNUM}",
    "defaultIndex": "logstash-*",
    "dateFormat": "HH:mm:ss.SSS MM-DD-YYYY",
    "dateFormat:tz": "UTC"
  }
}
