[Unit]
Description=docker-volume-neon-volume
Before=docker.service
Wants=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker-volume-neon-volume

[Install]
WantedBy=multi-user.target
