<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp2.0</TargetFramework>
  </PropertyGroup>

  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Exec Command="docker run --rm -v $(ProjectDir)\..:/src neoncluster/golang $(ProjectName) go build&#xD;&#xA;if not exist &quot;$(NF_BUILD)&quot; mkdir &quot;$(NF_BUILD)&quot;&#xD;&#xA;if exist bin\$(ProjectName) copy bin\$(ProjectName) %NF_BUILD%" />
  </Target>

  <ItemGroup>
    <None Remove="main.go" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="main.go" />
  </ItemGroup>

</Project>
