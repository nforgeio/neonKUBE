apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: test.neon.kube
rules:
- apiGroups:
  - ""
  resourceNames:
  - test.neon.kube-webhook-tls
  resources:
  - secrets
  verbs:
  - watch
- apiGroups:
  - cert-manager.io
  resources:
  - certificates
  verbs:
  - '*'