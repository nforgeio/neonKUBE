<div class="py-2 px-3 space-x-1 rounded flex w-full @CardStyle[Type] @(Body!= null ? "" : "items-center")">
  @Left
  <div class="">
        @if (Title != "")
        {
          <label class="text-base font-bold ">
      @Title
    </label>
            
        }
        
    @Body
  </div>
  @Right
</div>