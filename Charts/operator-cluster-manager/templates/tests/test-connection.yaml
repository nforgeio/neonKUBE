apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operator-cluster-manager.fullname" . }}-test-connection"
  labels:
    app: {{ include "operator-cluster-manager.name" . }}
    helm.sh/chart: {{ include "operator-cluster-manager.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "operator-cluster-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
