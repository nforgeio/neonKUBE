apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-cluster-manager.fullname" . }}
  labels:
    app: {{ include "operator-cluster-manager.name" . }}
    helm.sh/chart: {{ include "operator-cluster-manager.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "operator-cluster-manager.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
