apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "m3db-cluster.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "m3db-cluster.name" . }}
    helm.sh/chart: {{ include "m3db-cluster.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "m3db-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
