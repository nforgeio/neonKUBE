# Default values for m3db-cluster.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

isolationGroups: []

containerResources:
  requests:
    storage: 1Gi
  limits:
    storage: 1Gi
    cpu: 500m
    memory: 512Mi

volumeResources:
  requests:
    storage: 1Gi
  limits:
    storage: 1Gi

replicationFactor: 1